table! {
    users (id) {
        id -> Int4,
        first_name -> Varchar,
        last_name -> Varchar,
        username -> Varchar,
        password -> Varchar,
        email -> Varchar,
        joined_at -> Timestamp,
    }
}
